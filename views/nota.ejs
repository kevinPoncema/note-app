<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detalle de la Nota</title>
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>

<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">Detalle de la Nota</h5>
                    <form action="/updateNote/<%= rows[0].id_nota %>" method="post">
                        <!-- Input para el título de la nota -->
                        <div class="form-group">
                            <label for="titulo">Título de la Nota</label>
                            <input type="text" class="form-control" id="titulo" name="titulo" value="<%= rows[0].titulo_nota %>">
                        </div>
                    
                        <!-- Input para las tags -->
                        <div class="form-group">
                            <label for="tags">Tags</label>
                            <input type="text" class="form-control" id="tags" name="tags" value="<%= rows[0].tags %>">
                        </div>
                    
                        <!-- Textarea para el contenido de la nota -->
                        <div class="form-group">
                            <label for="contenidoNota">Contenido de la Nota</label>
                            <textarea class="form-control" id="contenidoNota" name="contenido" rows="5"><%= rows[0].cnitenido_nota %></textarea>
                        </div>
                    
                        <!-- Botones de Guardar y Eliminar -->
                        <div class="form-group d-flex justify-content-between">
                            <button type="submit" class="btn btn-primary">Guardar</button>
                        </div>
                    </form>
                    <form action="/deleteNote/<%= rows[0].id_nota %>" method="get">
                        <button type="submit" class="btn btn-danger">Eliminar</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Bootstrap JS -->
<script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.4/dist/umd/popper.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
